{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npx expo export --platform web",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!_expo|assets|favicon\\.png).*)", "destination": "/index.html" }
  ]
}
